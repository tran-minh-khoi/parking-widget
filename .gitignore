# ── Secrets and per-developer config: never commit ──────────────────────────
.env
.env.*
!.env.example
GoogleService-Info.plist
google-services.json
*.p8
*.p12
*.pem
*.key
*.jks
*.mobileprovision
*service-account*.json
*-adminsdk-*.json

# ── Dependencies ─────────────────────────────────────────────────────────────
node_modules/

# ── Expo / React Native ──────────────────────────────────────────────────────
.expo/
dist/
web-build/
expo-env.d.ts
.kotlin/
.metro-health-check*

# Native projects are generated by `expo prebuild` (CNG); the sources of truth are app.json + plugins
/ios
/android

# ── Firebase local state ─────────────────────────────────────────────────────
.firebase/
*-debug.log
firebase-debug.*.log
.runtimeconfig.json

# ── Logs, editor and OS files ────────────────────────────────────────────────
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo
.DS_Store
.idea/
.claude/
